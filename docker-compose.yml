services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: basketball
      POSTGRES_PASSWORD: basketball
      POSTGRES_DB: basketball
    ports:
      - "5433:5432"
    volumes:
      - basketball_pgdata:/var/lib/postgresql/data

volumes:
  basketball_pgdata:
